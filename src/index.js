import ReactDOM from 'react-dom/client';

import './index.css';
import MoviesApp from './components';

const root = ReactDOM.createRoot(document.getElementById('container'));
root.render(<MoviesApp />);
